# Copy to .env on the VPS and fill in. Lives at /opt/voiceengine/evolution/.env
SERVER_URL=https://wa.auto4you.in
# openssl rand -hex 24
AUTHENTICATION_API_KEY=change-me
POSTGRES_DB=evolution
POSTGRES_USER=evolution
# openssl rand -hex 16
POSTGRES_PASSWORD=change-me
